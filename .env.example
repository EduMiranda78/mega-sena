# Gere uma chave exclusiva antes de executar em produção.
# Exemplo: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=substitua_por_uma_chave_aleatoria_e_secreta

# Use true somente quando a aplicação estiver disponível por HTTPS.
SESSION_COOKIE_SECURE=false
